{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DNS Settings",
  "type": "object",
  "properties": {
    "primary_dns": { "type": "string", "format": "ipv4" },
    "secondary_dns": { "type": "string", "format": "ipv4" }
  },
  "required": ["primary_dns"]
}
